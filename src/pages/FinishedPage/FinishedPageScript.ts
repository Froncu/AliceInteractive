import { defineComponent } from 'vue';

export default defineComponent({
  name: 'FinishedPage',
  setup() {
    // No need for any specific functionality here, since we're just displaying a static message
    return {};
  }
});
